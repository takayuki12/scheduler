import { defineConfig } from "windicss/helpers";

export default defineConfig({
  theme: {
    extend: {
      colors: {
        primary: "#ffee58",
        primaryDark: '#fdd835',
        grey: "#616161",
      },
    },
  },
});
